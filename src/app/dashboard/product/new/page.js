import { ProductCreateView } from 'src/sections/product/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Dashboard: Add a new booking',
};

export default function ProductCreatePage() {
  return <ProductCreateView />;
}
